import { ModalWrapper } from './modal-wrapper';
import { AddHeadsetForm } from '../../add-headset/form';

export default function AddHeadsetModal() {
  return (
    <ModalWrapper>
      <AddHeadsetForm />
    </ModalWrapper>
  );
}